package com.danielstiner.vibrates.view.components;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.LinearLayout;

public class PatternEditView extends LinearLayout {

	public PatternEditView(Context context) {
		super(context);
		init();
	}

	public PatternEditView(Context context, AttributeSet attrs) {
		super(context, attrs);
		init();
	}

	public PatternEditView(Context context, AttributeSet attrs, int defStyle) {
		super(context, attrs);
		// TODO: super(context, attrs, defStyle);
		init();
	}

	private void init() {
	}

}
